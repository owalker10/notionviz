/* eslint-disable no-param-reassign */
import React from "react";

// resize: both;
// overflow: auto;

import { ContentContainer } from "../Components/MainLayout";

export default (): JSX.Element => {
  return <ContentContainer maxWidth="xl">Hello world</ContentContainer>;
};
